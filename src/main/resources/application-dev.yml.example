server:
  port: 80

spring:
  datasource:
    url: jdbc:mysql://REDROCK_CORE_DB_HOST:REDROCK_CORE_DB_PORT/REDROCK_CORE_DB_DBNAME?characterEncoding=utf8&useSSL=false&serverTimezone=UTC
    username:
    password:

  redis:
    host:
